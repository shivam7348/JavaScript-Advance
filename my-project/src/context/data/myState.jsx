import MyContext from './myContext'

function myState(props) {
    cost state  = {
        name: 
    }
  return (
    <MyContext.Provider>{
        {props.children}
    }</MyContext.Provider>
  )
}

export default myState